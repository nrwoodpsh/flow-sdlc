# CLAUDE.md

## 정체성

**`flow` 플러그인의 소스.** 지시서는 **설치되면 남의 컨텍스트에 실린다 — 길이가 곧 비용이다.**
쓰는 사람은 나와 팀이다. 기여자용 문서를 만들지 않는다.

## 워크플로우

검사 — 문서를 고쳤으면 `python3 scripts/lint-docs.py` · 훅을 고쳤으면 `bash scripts/tests/hooks.test.sh`.

- **여기서 flow 커맨드를 쓰지 않는다** — 내보내기는 `release` 스킬이 정본이다.
- **검사기 통과는 검증이 아니다** — 케이스를 안 더하면 통과 숫자가 그대로다. **훅은 되돌려서 실패하는지도 확인한다.** 과차단을 풀면 미탐이 난다.
- 전면 재작성은 묻고 한다. **정지 조건은 사용자가 정한다** — *"수렴했다"* 는 전망이지 근거가 아니다.
- 두 매니페스트의 `version`·`description`은 같아야 한다. `scripts/bump-version.sh`는 `patch`·`major`만 받으니 **가운데 자리(0.7 → 0.8)는 손으로 같이 고친다.** 올릴지는 `release`가 묻는다.

## 가드레일

**기계가 막는 것과 약속인 것을 가른다.** 어느 쪽인지 모르면 약속으로 읽는다. **목록을 여기 옮겨 적지 않는다.**

| | 무엇 | 목록의 정본 |
|:--|:--|:--|
| 기계 | 되돌릴 수 없는 git·gh 명령 · 그 한계 | **`guard-danger.sh` 머리말** |
| 기계 | 코드↔문서 드리프트 *(사용자 프로젝트만)* | `drift-check` 스킬 |
| 약속 | 자동 커밋 · 운영 MCP 쓰기 | 아래 |

- push·merge는 사람이 외부 툴로. 훅은 **무심코 치는 것을 막는 장치**다.
- 운영 MCP 쓰기 금지 — **읽기 전용 계정이 유일한 방어다.** 훅으로 막아 봤지만 판정이 양방향으로 틀려 걷어냈다.
- **방어는 커밋되는 자리에 둔다** — `.git/hooks/`는 조용히 낡아 아무것도 안 막는다.

## 더 읽을 것

`plugins/flow/**`·`guide/*.md`·`README.md`를 고치면 `.claude/rules/plugin-authoring.md`가 자동으로 실린다 — `바꿀 때 같이 고칠 곳`이 연쇄의 정본이다. **이 파일과 `scripts/`는 그 `paths`에 없다** — 열어 본다.
