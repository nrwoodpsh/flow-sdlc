# spike — 버릴 검증 코드 자리

`/flow:spike`가 주제별 `00.{주제}/`(넘버링) 아래 throwaway 코드로 가설을 검증한다.
- **prod 승격 금지** — 참조·아이디어만, 자산은 깨끗이 재작성.
- 결과(판정·제약)는 `doc/02.decisions`(ADR) + `doc/00.ref`로 승격.
- 이 폴더 내용물은 `.gitignore` 대상(README만 추적).
