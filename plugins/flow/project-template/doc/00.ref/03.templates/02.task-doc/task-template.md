---
requirement: [{{USER-LOGIN-1}}]
design: [{{D-1}}]
---

# {{NN}}.{{name}} — {{한 줄 제목}}

> 유닛: {{domain}}/{{NN.unit}} · 브랜치: {{branch}}
> `/flow:spec` 산출물. **무엇을 만드나**만 적는다.
>
> **여기 없는 것** — 왜 만드나(요구·완료 기준)는 `0.requirement.md`, 어떤 구조인가(설계·결정·화면)는 `1.design.md`,
> 타입·엔드포인트·에러코드는 `3.contract/`. **복사해 오지 않는다** — 고칠 곳이 둘이 되면 어긋난다.

## 1. 사용 계약  **[진행 필수]** — 단 **계약을 쓰는 task만**

계약은 여러 task가 공유(M:N)한다. 이 task가 **무엇을 쓰는지** 여기서 추적한다.

> **여기가 유일한 추적 자리다.** 비면 계약을 고칠 때 **영향받는 task를 못 찾는다**(`impact-analysis`). 계약을 안 쓰는 task(순수 계산·배선)는 `없음`으로 적는다.

| 계약 | 쓰는 부분 |
|:--|:--|
| `3.contract/{{도메인}}.ts` | {{로그인 요청/응답 DTO · 에러코드 U001}} |

## 2. Logic  **[진행 필수]**

구현 수준의 알고리즘·계산식·쿼리. 구조·책임은 `1.design.md`에 있다.

{{비번 해시 대조 → 실패 카운터 증가 → 임계 넘으면 잠금 → 통과 시 JWT 발급(30분)}}

## 3. File Map  **[진행 필수]**

기계적 — 판단이 필요 없다.

- `[New] {{경로}}` — {{역할}}
- `[Mod] {{경로}}` — {{변경 내용}}

> `[Mod]`가 있으면 `/flow:build`가 `impact-analysis`로 회귀 표면을 뽑는다.

## 4. Verification  **[진행 필수]**

**어떻게 확인하나.** 무엇이 완료인가(완료 기준)는 `0.requirement.md`에 있다.

- 명령: {{`npm test -- login`}}
- 통과 조건: Exit 0 · 덮는 요구({{USER-LOGIN-1}})의 완료 기준 충족
- **흔들리면 횟수·임계**: {{10회 중 8회 이상 · 해당 없으면 "1회 결정론"}}
- **비용 상한**: {{LLM 10회 · 1회당 약 2원 · 해당 없으면 "없음"}}

> LLM처럼 결과가 매번 다르면 **횟수·임계를 적는다** — 정하면 Exit code로 판정된다. 돈이 들면 **상한도 적는다**(`test-spec`).

## 5. History

`/flow:build`·`/flow:sync`가 갱신. 구현 중 **계획에서 벗어난 지점은 이탈(Deviation)로 기록** — 계획이 말한 것 / 코드가 드러낸 것 / 택한 선택 / 사유.

> **이탈의 정본이 여기다.** `4.build`엔 한 줄만 두고, `/flow:sync`·`/flow:review`·재개(`/flow:ask`·`/flow:run`)가 이 표를 읽는다. 같은 내용을 두 곳에 적지 않는다.

| 일시 | 단계 | 내용 |
|:---|:---|:---|
| {{YYYYMMDD HH:MM}} | `/flow:spec` | 최초 작성 |
| {{YYYYMMDD HH:MM}} | `/flow:build` · Deviation | {{계획은 토큰이 항상 있다고 가정 → 레거시 행에 없음. 없으면 0 처리(보수적). 사유: 마이그레이션 전 데이터 혼재}} |
