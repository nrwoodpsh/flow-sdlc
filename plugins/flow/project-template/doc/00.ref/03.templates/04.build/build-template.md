# 4.build/{{NN}} — {{task 이름}} 구현 기록

> task: `2.task/{{NN}}.{{name}}.md` · 브랜치: {{branch}}
> `/flow:build` 산출물. 무엇을 어떻게 구현했나. **task와 번호 1:1.**

## 구현 요약  **[문서 필수]**

{{핵심 로직·접근을 2~3줄}}

## 변경 파일  **[문서 필수]**

- `[New] {{경로}}` — {{역할}}
- `[Mod] {{경로}}` — {{변경 내용}}

## 계약 준수  **[진행 필수]**

- 계약 `3.contract/{{도메인}}.ts` — {{준수 여부 · contract-gate 통과}}

## 이탈 (Deviation)  **[진행 필수]**

> **상세는 task의 `History`에 적는다** — 거기가 정본이다(`/flow:sync`가 그걸 읽어 문서를 맞춘다).
> 여기는 **있었나·무엇이었나 한 줄**만. 두 곳에 같은 내용을 적으면 고칠 곳이 둘이 된다.

- {{없음}} 또는 {{토큰 없는 레거시 행 처리 — 상세: `2.task/{{NN}}.{{name}}.md` History}}
