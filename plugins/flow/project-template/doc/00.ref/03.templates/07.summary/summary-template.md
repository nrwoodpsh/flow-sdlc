# 7.summary — {{유닛}} 요약 ({{YYYYMMDD}})

> 유닛: {{domain}}/{{NN.unit}} · 브랜치: {{branch}} · 회차: {{NN}}
> `/flow:sync` 산출물. 이번 작업이 무엇을 바꿨나 롤업. 언어는 `workflow.config`의 `language`.

## 개요

{{무엇을 했나 2~3줄}}

## 변경

- **BE**: {{...}}
- **FE**: {{...}}
- **DB**: {{... / 없음}}

## API 변경

- {{엔드포인트·계약 변경 / 없음}}

## 특이사항

- 설계 대비 변경: {{이탈 요약 / 없음}}
- 제약 · 후속: {{알아둘 것 · 다음 작업}}

## 발행

`/flow:publish`가 남긴다. **다음 발행이 갱신인지 신규인지를 이 URL로 판정**하므로 지우지 않는다.

- **유닛당 페이지는 하나다.** 회차가 쌓여도 같은 페이지를 갱신한다 — 회차마다 같은 URL이 반복되는 게 맞다.
- **`/flow:publish`는 전 회차를 뒤져 URL을 찾는다.** 이 회차에 URL이 없어도 이전 회차에 있으면 그 페이지를 갱신한다.

- {{Notion}} — {{URL}} ({{YYYYMMDD}})

## History

- {{YYYYMMDD}} {{변경 내용 / 사유}}
