# 이 폴더의 템플릿이 복사된 시점의 flow 버전
#
# /flow:setup 이 복사할 때 채우고, 업데이트 모드에서 이 값을 플러그인 버전과 비교한다.
# 값이 다르면 setup 이 바뀐 템플릿을 파일별로 보여주고 선택을 받는다.
#
# 손으로 고치지 않는다 — 고치면 동기화 판정이 틀어진다.

flow: {{플러그인 버전}}
복사: {{YYYYMMDD}}

# 프로젝트가 일부러 고친 템플릿을 여기 적어두면 업데이트에서 건너뛴다.
# (setup 이 물을 때 "고쳤다"고 답하면 자동으로 추가된다)
고친 것:
#  - 02.task-doc/task-template.md   사내 규약에 맞춰 Verification 절 확장
