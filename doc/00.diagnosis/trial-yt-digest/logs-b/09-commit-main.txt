변경 목록과 커밋 전 확인을 정리했습니다.

## 변경 목록

**문서 (`/flow:sync` 산출)**
| 파일 | 내용 |
|:--|:--|
| `doc/01.work/README.md` | 유닛 인덱스 채움 (`collect/00.quota` — 검증됨) |
| `.../00.quota/0.requirement.md` | 빌드 실패 원인 정정 (`typescript` 미설치 → Exit 127) |
| `.../00.quota/1.design.md` | 같은 정정 + History |
| `.../2.task/00.quota-counter.md` | 같은 정정 + `/flow:build` History |
| `.../2.task/01.fetch-guard.md` | 같은 정정 + `/flow:build` History |
| `.../00.quota/README.md` | 신규 (유닛 인덱스) |
| `.../00.quota/7.summary/00.요청-전-상한.20260810.md` | 신규 (요약) |

**소스**
| 파일 | 내용 |
|:--|:--|
| `src/collector/quota.ts` | `+// drift-hook 실측용 주석 (B)` — **주석 한 줄뿐. 로직 변경 없음** |

**시험 로그 (untracked)** — `_trial/06-sync.txt` · `07-commit-drift.txt` · `08-guard-probe.txt` · `09-commit-main.txt`(0바이트)

## 커밋 전 확인

- **드리프트** — 없음. 소스와 대응 문서가 같이 왔습니다. `core.hooksPath=.githooks` 가 걸려 있어 `pre-commit` 훅도 돕니다.
- **스키마 문서** — 해당 없음. 마이그레이션이 없습니다.
- **리뷰** — ⚠ **`6.review/` 디렉토리가 아예 없습니다.** 이 변경을 아직 아무도 리뷰하지 않았다는 뜻입니다. 소스 변경이 주석 한 줄이라 실질 위험은 낮지만, 사실대로 알립니다.
- **조합 검증** — 유닛이 하나(`collect/00.quota`)라 알리지 않습니다.
- **계약** — `3.contract/collect.ts` 는 이번 변경에 안 들었습니다(이미 커밋됨).
- **민감정보** — 파일 이름 기준으로 `.env`·`*.key`·`*-prod.*` 해당 없음. 통과.

## 진행 전에 물을 것 셋

**1. 커밋 범위** — 두 가지가 모호합니다.
- `src/collector/quota.ts` 의 `// drift-hook 실측용 주석 (B)` 는 훅 동작을 실측하려고 넣은 표식으로 보입니다. **이걸 이력에 남길까요, 아니면 빼고 문서만 커밋할까요?**
- `_trial/06~09.txt` — `01~05` 는 이미 추적 중이니 같은 시리즈로 넣는 게 일관됩니다. **넣을까요?** (`09-commit-main.txt` 는 0바이트입니다)

**2. 브랜치** — 지금 `main` 입니다. 기본 브랜치에 직접 커밋하지 않으므로 새 브랜치를 만들어야 합니다. 이름을 확인받고 만들겠습니다.

**3. Git 규약** — `CLAUDE.md` 의 브랜치·커밋 메시지 형식이 비어 있습니다. 처음 커밋하는 지금이 정할 시점입니다. "아무거나" 라고 하시면 아래를 기본값으로 쓰고 `CLAUDE.md` 에 적어 다음부터 묻지 않겠습니다.

- 브랜치: `<타입>/<도메인>-<유닛>` → 이번 건은 `docs/collect-quota-sync`
- 커밋 메시지: Conventional Commits (`docs: ...` · `feat: ...` · `fix: ...`)

답을 주시면 그대로 커밋하겠습니다. push·merge 는 하지 않습니다.
