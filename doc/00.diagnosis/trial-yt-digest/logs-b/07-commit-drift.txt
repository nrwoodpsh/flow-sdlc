변경 목록과 커밋 전 확인을 마쳤습니다. **드리프트에서 막힙니다.**

## 변경 목록

| 구분 | 파일 | 무엇 |
|:--|:--|:--|
| 소스 | `src/collector/quota.ts` | +2줄 — 파일 끝 주석 `// drift-hook 실측용 주석 (B)` |
| 미추적 | `_trial/06-sync.txt` | `/flow:sync` 리포트 |
| 미추적 | `_trial/07-commit-drift.txt` | **빈 파일**(0바이트) |

## 커밋 전 확인 (넷)

**드리프트 — 걸립니다.** `src/collector/quota.ts` 는 `drift.sourceGlobs` 의 `src/**` 안이라 소스로 잡히고, `doc/01.work/` 아래는 아무것도 안 왔습니다. 훅은 `core.hooksPath=.githooks` 로 걸려 있고 `doc/01.work/collect/00.quota` 가 있어 검사가 켜진 상태입니다 — **`pre-commit` 이 `exit 1` 로 막습니다.**

- 내용은 주석 2줄뿐이라 절차상으로는 "그대로 진행" 자리지만, **훅은 내용을 안 보고 경로로만 판정합니다.** 확인 단계에서 봐주는 것과 훅이 막는 것은 별개입니다.
- `_trial/` 은 `sourceGlobs` 밖이라 소스로도 문서로도 안 잡힙니다 — 판정에 영향 없습니다.

**스키마 문서** — 마이그레이션 없음. 해당 없습니다.

**리뷰** — `doc/01.work/collect/00.quota/6.review/` 가 **없습니다.** 이 유닛은 아직 리뷰 기록이 한 번도 없습니다. 다만 이번 변경은 주석 한 줄이라 새로 볼 코드가 없습니다.

**조합 검증** — 유닛 하나(`collect/00.quota`)라 알릴 것이 없습니다.

## 멈춘 자리

`--no-verify` 는 붙이지 않습니다. 세 갈래입니다.

1. **`/flow:sync` 를 돌린다** — 문서를 맞추고 같은 커밋에 담습니다. 다만 06-sync 리포트를 보면 sync 는 직전에 이미 수렴을 끝냈고, 이번 주석 2줄로 갱신할 설계 내용이 실제로는 없습니다. 형식만 채우게 될 수 있습니다.
2. **여기서 멈춘다** — 넘길지는 직접 터미널에서 `git commit --no-verify` 로 판단하십시오. AI 경로에서는 가드가 그 플래그를 막습니다.
3. **주석을 되돌린다** — `// drift-hook 실측용 주석 (B)` 가 실측 목적이었다면, 훅이 막는 것을 확인한 시점에서 목적은 달성됐습니다.

## 함께 정할 것 (드리프트가 풀리면)

- **`CLAUDE.md` 의 Git 규약이 비어 있습니다** — 브랜치 이름·커밋 메시지 형식. 처음 커밋할 때 한 번 정해 적습니다. ("아무거나" 라고 하시면 기본값을 쓰고 무엇으로 정했는지 `CLAUDE.md` 에 적습니다.)
- **현재 `main` 입니다** — 기본 브랜치에 직접 커밋하지 않습니다. 새 브랜치 이름을 확인받아야 합니다.
- **`_trial/` 2개를 커밋에 넣을지** — 특히 `07-commit-drift.txt` 는 빈 파일입니다.

어느 갈래로 갈까요?
