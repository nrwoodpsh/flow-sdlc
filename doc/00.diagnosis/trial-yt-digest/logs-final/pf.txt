## 정리 결과

**레벨** 기능 · **대상** `collect/00.quota` · **착지** `doc/01.work/collect/00.quota/0.requirement.md`

### 새 요구 ID: 0건

빠져 있던 것이 목표가 아니라 **규칙**이었습니다. `usecase` 의 입도 규약이 "실패·예외를 별도 유스케이스로 빼지 않는다"고 하므로, 구현·검증돼 있으나 요구에 없던 둘을 업무 규칙으로 이어 붙였습니다.

- **R6** — 차단 시 소비량과 다음 창이 열리는 시각을 알린다 (`channel.test.ts:64` 가 이미 검증 중)
- **R7** — 한 호출이 돌려주는 항목은 100건까지 (현행 유지, `channel.test.ts:121` 회귀)

`범위 밖` 의 "응답 자르기" 항목은 *정책 변경*으로 좁혀 R7 과 겹치지 않게 했습니다.

### 외부 쿼터 대조 — `확인 필요`

**`? ÷ ? = 100`** — 우변만 있습니다. 배정 쿼터 값과 `search` 1회 사용량이 리포에 없고(`gatekeeper` 가 grep 으로 0건 확인), **단위도 어긋납니다**: 우리 상한은 *호출 횟수*인데 제공자 쿼터는 호출별 소모량으로 매겨질 수 있어 이 식으로는 안 갈립니다. 숫자를 지어 넣지 않았습니다.

`충돌` 이 아니므로 멈추지는 않습니다 — 상한을 요청 전으로 옮기는 것은 값이 얼마든 옳습니다.

### 정정한 것

- `의존` — 빌드 실패 원인이 `tsconfig.json` 부재라고 적혀 있었지만, **먼저 걸리는 것은 `typescript` 미설치**입니다 (실행 확인: `Exit 127 tsc: command not found`)
- `배경` 절이 이미 사라진 주석을 현재 시제로 가리키던 것
- `COLLECT-QUOTA-1` 의 액터 칸에 연동 시스템 누락

### ℹ️ 템플릿에 없는 절

`외부 의존 · 쿼터 대조` 절이 프로젝트 요구 템플릿에 없습니다(`의존` 표에 쿼터·대조 칸이 없음). 되묻지 말라고 하셨으므로 **①이 문서에만 절을 추가**하고 진행했습니다. **템플릿이 기준이라 이 절은 채점되지 않습니다** — 동기화는 `/flow:setup` 의 몫입니다.

### `gatekeeper` 판정 — 그리고 미완인 것

**BLOCK 이 한 번 났고, 사유가 타당해서 고쳤습니다.** 문서가 시스템 레벨 미결(`DAILY_LIMIT` 이 쿼터 보호인가)을 "확정됐다"고 선언하면서 그 확정이 뒤집는 레벨 판정은 미뤘고, 받을 문서로 인계되는 경로가 리포에 없었습니다. 확정 문장을 근거 제공으로 낮추고, 권한 표(이 유닛이 정하는 것 ↔ `/flow:prd sys` 가 정하는 것)와 인계 지점 표를 넣었습니다. 시스템 요구 문서는 sys 레벨의 자리라 건드리지 않았습니다.

**재판정을 돌리지 못했습니다 — 예산이 소진됐습니다**($4 중 약 $0.2 남음, 앞선 판정에 약 $1.2). 규약상 *판정 뒤에 산출물을 고쳤으면 그 판정은 무효*이므로, **이 문서는 아직 게이트를 통과하지 않은 상태**입니다. 고친 것이 BLOCK 사유를 실제로 해소했는지는 제가 판정할 자리가 아닙니다.

다음 턴에 `gatekeeper` 재판정부터 돌리시면 됩니다. 통과하면 다음 국면은 `/flow:design` 입니다.

**함께 넘겨받을 별건** (게이트 대상 아님, `/flow:sync` 몫): `5.verify/01.md:39` 가 정정 전 줄번호를 인용해 stale · 문서 쪽 `DAILY_LIMIT` 잔존 11건.
