조각과 대상 문서를 다 읽었다. 채점 결과다.

```
[/flow:review doc] 대상: doc/01.work/collect/00.quota/  (유닛 하나 — explorer 미위임, delegation 범위 표)
                   기준: doc/00.ref/03.templates/ (프로젝트 템플릿)
                   기록: 파일 미생성 — 사용자가 "리포트만" 지시
                        (커맨드 기본은 6.review/NN.내용.YYYYMMDD.md 회차 기록이다)
ℹ️ 템플릿에 버전 표기가 없다 — 플러그인과 동기화 여부를 비교할 근거가 없다
ℹ️ 역추출 0건 — 요구 표 `상태` 칸이 둘 다 비어 있다(=확정). 신규 기준 전부 적용, reverse-check 미적용
```

**빠진 층 — 안 본 것을 "문제없다"로 읽지 마라**

| 무엇 | 왜 |
|:--|:--|
| **`gatekeeper` 반증** | 이 세션은 서브에이전트 호출이 금지돼 있다. 문서 모드는 `finding-severity` 등급이 아니라 PASS/FAIL 채점이라 반증 대상(critical·high)이 없지만, **판정 독립성은 없다** — 아래 등급은 내가 매기고 내가 확정했다 |
| 테스트 재실행 | `5.verify` 의 `Exit 0 · Tests 14/19 passed` 주장을 돌려 확인하지 않았다. 문서 대조만 했다 |
| 계약 파일 채점 | `3.contract/collect.ts` 는 채점 대상이 아니다(`contract-gate` 소관). 값 중복 대조용으로만 읽었다 |
| 회고 조치 등록 대조 | `04.ops/` 가 없다 — N/A |
| `plain-writing` 조각 | 안 읽었다. 문체 규약이라 채점 결과에는 영향이 없다 |

---

## 진행 필수 (없으면 `gatekeeper` 가 차단한다)

✅ **통과** — `0.requirement` 요구 목록·업무 규칙 / `1.design` 설계 요소(D-1·D-2 가 요구 둘을 덮는다) / `2.task` frontmatter 태그·사용 계약·Logic·File Map·Verification(두 task 전부) / `4.build` 계약 준수 / 요구↔설계↔task↔build↔verify 태그 사슬(`traceability/coverage`) / 완료 기준 측정 가능 / 측정 방법 기재 / 범위 밖↔요구 목록 대조 / PlantUML 레이아웃 지시 3개 전부 / 표 칸 수 일관(깨짐 0)

❌ **`5.verify/00.md`·`5.verify/01.md` — `판정` 절이 없다** (2건)
`verify-template.md:6-14` 는 판정을 **맨 앞 절**로 두고 `결과: / Pass / Fail / Exit` 형식을 요구한다. 두 문서는 `## 명세` → `## 결과` 순이고 판정 절이 없다. `doc-verify/grade.md:24` — **`gatekeeper` 가 PASS/FAIL 을 여기서 읽는다.** 정보 자체는 `결과` 절에 있다(`00.md:17`·`01.md:17`)지만 읽을 자리에 없다.
→ **`/flow:verify`**

❌ **`5.verify/00.md`·`5.verify/01.md` — `완료 기준 매핑` 절이 없다** (2건)
두 문서의 결과 표 칸은 `task 가 적은 경계 | 케이스 | 판정` 이다. 템플릿(`verify-template.md:25`)이 요구하는 **`요구 | 기대값 출처 | 테스트 | 결과`** 가 아니라 **요구 ID 칸과 기대값 출처 칸이 없다.** 커버리지 감사가 요구 ID 로 모아 갈 자리가 사라진다. `01.md:23` 이 "COLLECT-QUOTA-2 의 완료 판정 자리"라고 산문으로 적어 둔 것이 이 절이 해야 할 일이다.
→ **`/flow:verify`**

❌ **`5.verify/00.md`·`5.verify/01.md` — `미검증` 절이 없다** (2건)
`verify-template.md:43` — **"없으면 `없다` 로 적는다. 절을 지우지 않는다"** · `:44` **"산문으로 쓰지 않는다. 표가 아니면 상위 범위가 못 모은다."** 두 문서는 `결함 · 남는 것` 에 산문으로 섞었다.
**이 절이 없어서 실제로 빠진 것이 있다** — `0.requirement.md:61` **R2**(카운트 단위는 API 키 전역)와 `:64` **R5**(프로세스 메모리 한계)를 덮은 테스트가 두 task 의 Verification 경계 목록에도, `5.verify` 결과 표에도 없다. R5 는 구조상 이 범위에서 검증 불가인데 `미검증` 표가 없어 상위 범위로 넘어가지 않는다.
→ **`/flow:verify`**

❌ **`4.build/00.md:31` — 가리킨 task `History` 기록이 실재하지 않는다**
"판단이 갈릴 수 있는 지점 둘은 task `History` 에 Deviation 없이 기록만 남긴다" 고 적었으나, `2.task/00.quota-counter.md:74-76` History 표에는 `20260809 · /flow:design · 최초 작성` 한 줄뿐이다. `build-template.md:21` — **이탈의 정본은 task History** 이고 `/flow:sync`·`/flow:review`·`/flow:next` 가 그 표를 읽는다. 지금은 `4.build` 만 아는 판단 둘이 정본에 없다.
→ **`/flow:build`** (task History 에 기록) 또는 **`/flow:sync`**

❌ **`0.requirement.md:62` R3 — 근거로 든 크로스커팅 UTC 규약이 리포에 없다**
R3 는 창 시작을 UTC 자정으로 정한 근거를 "크로스커팅 UTC 규약(`CLAUDE.md` 의 `참조 통제`)" 으로 댔고 `1.design.md:9` 도 "시각은 UTC (`CLAUDE.md`)" 로 받는다. 확인 결과:
- `CLAUDE.md:27` 은 **"`00.ref/00.architecture/` 에 크로스커팅 제약(UTC·금액단위 등)이 있다"는 참조 통제 안내**다. 규약 본문이 아니다.
- `00.ref/00.architecture/00.requirement.md` 요구 표에 시각 관련 `SYS-*` 가 **없고**, `:26` 이 **"코드에 시각 규약이 없어 올릴 횡단 제약이 없다"** 고 직접 적었다.
- `00.ref/00.architecture/` 에는 `00.requirement.md` 뿐이다 — 아키텍처 본문 문서가 없다. UTC 문구는 템플릿 예시(`13.architecture/architecture-template.md:230`)에만 있고 템플릿은 정본이 아니다.

`usecase/rule-level.md:24,36` — 여러 도메인에 걸리는 규칙은 아키텍처의 **`SYS-번호`** 로 올리고 `참조:` 로 **번호만** 가리킨다. 지금은 없는 규약의 내용을 R3 본문에 적었다. 창 경계라는 실제 설계 선택의 근거가 비어 있다.
→ **`/flow:prd sys`** (SYS 발급) + **`/flow:prd`** (기능 — R3 를 `참조: SYS-N` 으로 교체)

⚠️ **`0.requirement.md:25-26` — `종류: 원가` 인데 액터·주 흐름·예외·상세를 다 채웠다**
`usecase/granularity.md:36-39` 의 예시는 국지 비기능 요구의 액터·주 흐름·예외를 **`—`** 로 둔다(액터도 흐름도 없는 제약이라서). 두 요구는 기능 UC 의 칸과 원가 제약의 칸을 한 행에 뭉갰다.
**따라오는 결과** — `usecase/figure-scope.md:32` 의 그림 대상 판정표는 `종류` 가 `원가` 인 행을 **대상이 아니라고** 가른다. 두 행 다 `원가` 라 **그림 대상 행이 0개**가 되고, `유스케이스 그림` 절의 이름 집합 대조가 성립하지 않는다(그래서 아래 액터 누락을 그림 대조로는 판정하지 못했다).
→ **`/flow:prd`** (기능) — 기능 UC 행과 원가 요구 행을 가를지 사람이 정한다

❌ **`0.requirement.md:25` — 요구 표 액터 칸에 연동 시스템이 없다**
`usecase/granularity.md:28` — 액터 칸은 **주 액터 · 연동 시스템**이다. `## 액터` 절(`:19`)에는 `유튜브 Data API v3` 가 보조 시스템으로 있고 그림(`:44`)도 `UC1 -- YT` 로 그리는데, **요구 표 액터 칸은 `수집기` 뿐이다.**
→ **`/flow:prd`** (기능)

❌ **`0.requirement.md:45` — 그림 관계 표기가 규약과 어긋난다**
`UC1 ..> UC2 : <<include>>` 로 그렸다. `«include»` 는 **항상 포함**이고(`usecase/figure-scope.md:18`) 리셋은 경계를 넘을 때만 일어난다 — `1.design.md:100`·`2.task/00.quota-counter.md:35` 의 판정식이 조건 분기다. 조건부면 `«extend»` 이고 방향이 `UC2 ..> UC1` 로 뒤집힌다. 더해 `figure-scope.md:23` — **"관계를 남발하지 않는다. 두 유스케이스가 같은 절차를 공유할 때만 `«include»`"**. UC2 에는 액터 연결선도 없다.
→ **`/flow:prd`** (기능)

⚠️ **`0.requirement.md:26` COLLECT-QUOTA-2 의 입도** — 확인 필요
"하루 경계에서 호출 카운트가 리셋된다" 는 `:56` 의 **UC1 대안 흐름 A1**("창 경계를 넘었으면 카운트를 0으로 되돌리고 소비한다")과 같은 내용이다. `usecase/granularity.md:16` 판단 기준 — *사용자가 "이걸 하러 왔다" 고 말할 수 있나.* 카운터 리셋은 시스템 내부 상태 변화라 목표로 읽기 어렵다.
**같은 판단이 이미 다른 문서를 무겁게 만들었다** — `1.design.md:125` 가 "COLLECT-QUOTA-2 를 D-1·D-2 가 함께 덮는다" 를 한 문단으로 변호하고, `2.task/00.quota-counter.md:67` 과 `01.fetch-guard.md:63` 이 "완료 판정 자리는 여기가 아니다" 를 서로 가리킨다. 요구 하나를 둘로 나눈 값이 이 배선이다.
다만 원가 요구로서 "나간 요청 수" 라는 별도 측정 대상을 갖는다는 문서 쪽 근거(`1.design.md:125`)도 실재한다 — **어느 쪽이 맞는지는 사람이 정한다.**
→ **`/flow:prd`** (기능)

⚠️ **`0.requirement.md:64` R5 는 업무 규칙이 아니다**
"카운터는 프로세스 메모리에 산다 · 재시작하면 창이 새로 열린다" 는 판정 기준·한도·예외 대상이 아니라 **설계 결정의 결과**다(`usecase/rule-level.md:14`). R5 스스로 `1.design.md` 의 결정 D-1 을 근거로 가리켜 **요구가 설계를 참조하는 역방향**이 됐다. `testing` 이 여기서 뽑을 경계값이 없고, `4.build/00.md:31` 도 "논리적으로 도달 불가라 단독으로 태울 테스트가 없다" 고 적었다.
→ **`/flow:prd`** (기능 — R5 를 규칙에서 내리고 `1.design.md` 결정·`범위 밖` 으로 보낸다)

---

## 문서 필수 (표준이 요구한다 · 차단은 안 한다)

✅ **통과** — `0.requirement` 액터 절(UML 유스케이스) / `1.design` 결정·기각 대안 9행(ISO/IEC/IEEE 42010 rationale · MADR) / `4.build` 변경 파일

⚠️ **`5.verify/00.md:30`·`01.md:41` — `결함` 절 이름·형식 불일치** (ISO/IEC/IEEE 29119-3 Incident Report)
`결함 · 남는 것` 으로 합쳤고 템플릿의 `결함 | 재현 | 증거 | 처리` 표가 아니라 목록이다. 두 문서 다 PASS 라 결함 0건이니 **표가 비는 것 자체는 정상**이지만, 절 이름이 달라 앵커로 가리킬 수 없다.
→ **`/flow:verify`**

⚠️ **`4.build/00.md:9`·`01.md:9` — `구현 요약` 절 이름이 `무엇을 만들었나`·`무엇을 바꿨나` 다** (2건)
내용은 있다. 이름만 다르다.
→ **`/flow:build`**

---

## 선택

— `1.design` 화면 구조(프론트 없음 · 명시) · `5.verify` 특이사항(`01.md:27-31` 에 순서 의존·네트워크 미발생으로 있다) · `2.task` History 의 Deviation 행(없음)

---

## 값 중복 · 정본 (`doc-verify/canon-map`)

| 값 | 어디에 | 정본 | 판정 |
|:--|:--|:--|:--|
| 호출 상한 `100` | `0.requirement.md:25` · `2.task/00:45` · `4.build/00:13` · `quota.ts:22` | **없다** — 요구 문서 스스로 `:29` 에서 "이 요구가 정하지 않는다" 고 적었다 | **`확인 필요`** — `canon-map.md:31` 대로 지어내지 않는다. 네 문서가 이미 `0.requirement.md:86` 의 `확인 필요` 를 가리키고 있어 **새 결함으로 올리지 않는다** |
| `MAX_ITEMS_PER_CALL = 100` | `2.task/01:39` · `4.build/01:15` · `channel.ts:17` | `channel.ts` 고정 상수 (`3.contract/collect.ts:65-67` 이 명시) | PASS — task 의 값은 `지금 → 바뀐 뒤` 대조표라 필요하다 |
| 완료 기준·상한 값 | `3.contract/collect.ts:5` 이 "여기 두지 않는다" 고 선언하고 숫자를 안 박았다 | `0.requirement.md` | ✅ |

---

## 사실 확인 — 등급 판정이 아니다

- **`SYS-2` 는 `상태: 확인 필요`(역추출·미확정)다** (`00.ref/00.architecture/00.requirement.md:24`). 이 유닛의 **확정** 요구가 그것을 예외 흐름 E2 의 근거(`0.requirement.md:57,65`)·설계 제약(`1.design.md:9,102,134`)·구현(`2.task/01:27`)으로 쓴다. 사람 검수에서 SYS-2 가 바뀌면 이 유닛의 E2 가 흔들린다.
- **고아 유지에 기한이 없다.** `0.requirement.md:6` 은 "상위: 없음(고아) — collect 도메인 문서가 아직 없다" 로 끝난다. `traceability/coverage.md:41,44` — **"`고아로 유지` 를 기한 없이 쓰지 않는다. 기한이 없으면 영구 노이즈가 된다"** (아키텍처 `확인 필요` 절에 `{언제까지}` 와 함께 적는다). 매 커버리지 감사가 같은 줄을 다시 낸다. → **`/flow:design`**(도메인) 또는 기한 등록
- **`SYS-*` 레벨 재판정이 어디에도 등록되지 않았다.** `00.ref/00.architecture/00.requirement.md:83` 이 "쿼터 보호가 의도로 확정되면 `SYS-*` 로 올라온다" 고 걸어 뒀고 `0.requirement.md:87` 이 **"이번에 그 의도가 확정됐다 · 재판정은 `/flow:prd sys` 의 몫"** 이라고 답했다. 그 커맨드를 돌린 흔적도 기한도 없다 — 회고 조치 미등록과 같은 모양이다. **위 R3 의 UTC `SYS` 발급과 한 번에 처리된다.** → **`/flow:prd sys`**
- **`0.requirement.md:69-73` 의존 표에 유튜브 Data API v3 행이 없다.** 이 요구가 지키려는 외부 쿼터의 값이 미확인인데(`00.architecture/00.requirement.md:36`) 의존 표는 `tsconfig`·저장 계층·호출부만 잡는다. `확인 필요` 절(`:85`)이 부분적으로 받고 있다. → **`/flow:prd`**
- **`7.summary/` 가 없다.** `/flow:sync` 산출이라 채점 대상이 아니다 — 유닛이 `0`→`5` 까지 와 있고 sync 전이다. `6.review/` 도 이 리뷰가 첫 회차라 없는 것이 정상이다.
- **이미 닫힌 지적은 다시 올리지 않았다** — 문서 쪽 `DAILY_LIMIT` 잔존 11건과 아키텍처 문서 낡은 서술은 `2.task/01:84`·`5.verify/01:31` 이 `/flow:sync` 몫으로 판단해 뒀다. `5.verify/01:39` 가 지적한 "실패 원인이 `tsconfig.json` 부재가 아니라 `typescript` 미설치" 도 그쪽에서 이미 sync 로 보냈다.

---

## 설명 노트 제안 — 채점 항목이 아니다

`doc/02.decisions/` 에 ADR 이 쌓이지 않았고(`1.design.md:139` 가 "지금은 아니다" 로 판단) 도메인 문서가 0개라, `ops-doc` 의 설명 노트 신호는 아직 서지 않는다. **먼저 필요한 것은 설명 노트가 아니라 `collect` 도메인 문서다** — 그것이 위 고아·`SYS` 레벨 재판정 둘을 함께 닫는다.

---

```
합계: 진행 필수 FAIL 9 (verify 6 · build 1 · requirement 2) · 확인 3
      문서 필수 FAIL 3 (이름·형식 불일치)
      사실 확인 4 · 값 중복 확인 필요 1
      역추출 0건 — 신규 기준 전부 적용

돌아갈 곳: /flow:verify 6 · /flow:prd(기능) 4+ · /flow:prd sys 1(R3 UTC + 레벨 재판정 묶음)
          /flow:build 3 · /flow:design(도메인) 1

고치지 않았습니다. gatekeeper 반증도 돌리지 않았습니다 — 판정 독립성 없음.
```

가장 무거운 것은 **`5.verify` 두 문서의 진행 필수 절 셋(`판정`·`완료 기준 매핑`·`미검증`)** 이다. `gatekeeper` 가 차단하는 항목이고, `미검증` 절 부재 탓에 **R2·R5 가 검증 목록에서 조용히 빠진 것**이 실제 손실로 이미 나타났다. 그다음이 **R3 의 UTC 근거 부재** — 창 경계라는 설계 선택의 유일한 근거가 실재하지 않는 규약이다.
