**시스템 레벨 설계 완료 — `gatekeeper` PASS.** 여기서 정지합니다(task 분할·계약 없음).

## 산출

- `doc/00.ref/00.architecture/01.design.md` — 신규
- `doc/00.ref/README.md` — 색인의 `00.architecture` 행이 `_(비어 있음)_` 으로 남아 있던 것을 두 파일 링크로 갱신

## 무엇을 정했나

| 절 | 결론 |
|:--|:--|
| 구성 요소 | **단일 배포 단위(미배포).** 도메인 셋 중 `collect` 만 `확인`, `summarize`·`digest` 는 `추정`, `lib/youtube/transcript.ts` 는 소속 `미정` |
| 의존 방향 | 확인된 간선 3개. 금지 방향 — 정책 모듈이 어댑터를 모른다 · 계약이 소스를 import 하지 않는다 |
| 크로스 도메인 흐름 | **부재를 사실로 기록.** 프로덕션 호출부 0을 전수 검색으로 확인했고, 잇는 흐름은 그리지 않았다 |
| 크로스커팅 | 시각(UTC) · 시각 주입 · 외부 실패는 예외 · 재시도는 횡단 아님. 에러 코드·로깅·페이징은 `미정` |
| 설계 요소 | `D-1` 실패의 예외 전파 경계 → `SYS-2`. 살아 있는 요구는 그 하나뿐이라 커버리지 gap 0 |
| 화면 지도 | 해당 없음 — FE 없음. `SCR-N` 발급 안 함 |

## 지어내지 않은 것

`package.json` 의 한 줄이 `수집 → 자막 → 요약 → 조회` 순서를 암시하지만, **그 순서를 요구로 올린 문서가 없어 흐름을 그리지 않았습니다.** `00.requirement.md` 의 `목표` 표가 전부 `확인 필요`인 상태에서 그리면 추정이 정본이 됩니다. 오케스트레이터·스케줄러 설계도 같은 이유로 기각 사유와 함께 미뤘습니다.

## 다음이 받아야 할 것

1. **`/flow:prd sys` — 쿼터 보호 제약의 `SYS-*` 승격.** `00.requirement.md:83` 이 걸어 둔 조건("의도로 확정되면 `SYS-*` 로 올라온다")을 `collect/00.quota` 유닛이 이미 충족시켰는데 발급이 안 됐습니다. 승격되면 `종류`가 `원가`라 이 설계에 **원가 측정 수단 `D-N` 이 하나 이상 붙어야 합니다** — 지금은 0개입니다.
2. **`/flow:prd domain`** — `summarize`·`digest` 경계 확정. 확정되면 구성 요소 표의 `담당 도메인` 칸이 함께 바뀝니다.
3. **`/flow:sync`** — `gatekeeper`가 별건으로 잡은 드리프트: `00.requirement.md` 의 `SYS-2` 근거가 `channel.ts:5`를 가리키는데 그 줄은 이제 JSDoc이고 실제 `throw`는 56행입니다. 유닛 빌드로 파일이 8줄→58줄로 자란 뒤 요구 문서가 안 따라왔습니다.

커밋은 하지 않았습니다 — 요청하실 때 `/flow:commit`으로 합니다.
