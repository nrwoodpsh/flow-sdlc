Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
## `/flow:prd sys legacy` 완료

**산출**: `doc/00.ref/00.architecture/00.requirement.md` (신규) · ID 형식 `SYS-N`

### 발급한 ID

| ID | 요구 | 상태 |
|:--|:--|:--|
| ~~SYS-1~~ | ~~HTTP API 는 인증을 요구하지 않는다~~ | ~~취소~~ — 표본 대조에서 내림 |
| SYS-2 | 현행: 외부 호출이 실패하면 예외를 던져 중단한다 | 확인 필요 |

**살아남은 시스템 요구가 하나입니다.** 코드에 인증·로깅·에러 코드·시각 규약이 없어 올릴 횡단 제약이 없었고, `traceability` 규약대로 억지로 만들지 않았습니다. `SYS-1` 은 번호를 비우지 않고 취소 표기로 남겼습니다 — 다음 발급은 `SYS-3` 부터입니다.

### 근거 등급과 대조 결과

앵커 13/13 실재 · 부재 주장 3건 확인 · 표본 6/6 전수(`verifier` 격리 판정). 코드가 이긴 곳이 셋입니다.

- **`SYS-1` 근거 없음** — `routes.ts:2` 는 라우트 이름을 핸들러 이름 **문자열**에 매핑한 객체뿐입니다. 서버·핸들러 심볼·미들웨어가 없어 "인증 없이 응답한다"를 참으로 만들 실행 경로가 없습니다. 확인된 것은 *인증이 구현돼 있지 않다* 이지 현행 동작이 아닙니다.
- **`SYS-2` 축소** — "부분 결과를 남기지 않는다"를 뗐습니다. 남길 저장 계층이 없어 공허하게 참일 뿐입니다.
- **재시도 수 오차** — 코드는 `for (i<3)` 이라 **총 3회 호출**(최초 1 + 재시도 2)입니다. "재시도 3회"는 4회로 읽혀 4곳을 고쳤습니다.

### 쿼터 대조 — 계산을 못 했습니다

세 외부 의존 모두 `현행 — {실제 동작}` 으로 채웠지만, **쿼터 값이 코드·설정·환경변수 어디에도 없어 나눌 숫자가 없습니다.** 유튜브 공식 쿼터를 기억으로 적으면 코드 근거가 아닌 추측이 요구 판정으로 굳으므로 넣지 않았습니다. `OK` 인지 `충돌` 인지는 사람이 값을 넣어야 갈립니다.

가장 큰 것: 주석은 "쿼터 때문에 하루 100건으로 막아 뒀다"인데 **코드에는 하루 단위 상태도 호출 횟수 제한도 없습니다.** `slice(0, 100)` 은 이미 받은 응답을 자르는 것이라 쿼터를 아끼지 못합니다.

### 게이트

`gatekeeper` — **PASS**(레벨 판정 · 쿼터 대조 둘 다). 다만 판정은 수정 **전** 문서 기준입니다. 이후 수정은 요구를 줄이고 `확인 필요` 를 늘린 방향이라 판정을 뒤집지 않지만, 재판정은 돌리지 않았습니다.

### 사람이 답해야 다음으로 가는 것

- 세 외부 의존의 쿼터·요금 — 이게 없으면 설계로 못 넘어갑니다
- 인증 없음이 결정인가 결함인가 (결정이면 `SYS-3` 으로 새로 발급)
- `DAILY_LIMIT` 이 쿼터 보호가 맞으면 **도메인이 아니라 `SYS-*` 로 올라옵니다** — 레벨 판정이 뒤집히는 자리
- 요약을 어디에 보관하나 (저장 계층·`02.db-schema/` 둘 다 비어 있음)
- `목표` 표 — 코드는 무엇을 하는지만 말하고 왜인지는 말하지 않습니다

역추출 결과는 **사람 검수 전까지 정본이 아닙니다.** 코드는 손대지 않았습니다.
